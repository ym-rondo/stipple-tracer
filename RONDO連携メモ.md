# RONDO 連携メモ

このファイルは、点描トレーサーを触るセッションが「RONDO 時の座標」側の事情を
把握できるようにするための引き継ぎメモ。新しいチャットを始めたら、まずこれを読む。

---

## この2つのアプリの関係

| | |
|---|---|
| **点描トレーサー**（このリポジトリ） | 画像 → 光の点描SVG を作る道具 |
| **RONDO 時の座標** | 3Dコズモロジー。19年 × 一年 × 月の入れ子構造を可視化する本体アプリ |

作った点描SVGを RONDO に載せる。**トレーサーは「絵を作る側」、RONDO は「絵を置く側」。**

- RONDO のリポジトリ: `ym-rondo/rondo-cosmos`（このPCでは同じ GitHub フォルダの隣）
- RONDO の公開先: https://rondo-cosmos.vercel.app
- このアプリの公開先: https://stipple-tracer.vercel.app

## いちばん大事な制約：光り方を揃えること

RONDO は three.js で作られた**光の点でできた空間**。そこへ別の色・別の光り方の絵を
持ち込むと、**その絵だけ浮いて「貼り付けた」ように見える**。実際に一度失敗している。

RONDO 側の光の組み立て（`makeGlowPoint` / `makeGlowTexture` より）:

    暈（ハロー） 色つき、不透明度 = 基準値 × 0.55、加算合成
    芯（コア）   純白 #ffffff、暈の 32% の大きさ、不透明度 = min(1, 基準値+0.2)、加算合成
    放射グラデーション  中心1.0 → 半径35%で0.5 → 外周0

**芯が白く小さいことが肝。** ここを外すと「光」ではなく「塊」に見える。

### RONDO で使っている色

| 用途 | 色 |
|---|---|
| 青（未来実装プロセス） | `#7fb4ff` |
| 赤（過去処理プロセス） | `#f08a72` |
| 金（現在地マーカー） | `#ffd479` |
| 生成り（意識世界の環） | `#f0ead8` |
| 青灰（現実世界の環） | `#aebedd` |
| 金茶（19年軌道の環） | `#d9bb7c` |
| 淡青（月の環） | `#cfd6e8` |

## グループ分けと、それぞれの色

ピクトグラムを何十個も作るとき、**同じグループのものは同じ設定で作りたい**。
RONDO 側にはすでにグループ分けと色があるので、それに合わせるとそのまま馴染む。
トレーサー側でプリセットを組むなら、この単位が使いやすい。

### 一年の区分（12個＋編成月）

| グループ | 含まれる区分 | 色 |
|---|---|---|
| **下降期** | 下降高次①② / 下降精神①② / 下降基底①② | `#d8cdb8` |
| **上昇期** | 上昇基底①② / 上昇精神①② / 上昇高次①② | `#b9c8e6` |
| **編成月** | 編成月（周期 1・3・6・9・11・14・17・19 のみ） | `#c9a0d9` |

さらに縦方向に3つのゾーンがある（高次／精神／基底）。
下降期・上昇期それぞれに 高次→精神→基底 の並びがあるので、
「高次のアイコン」「精神のアイコン」「基底のアイコン」という括り方もできる。

### 19年周期（19個）

| グループ | 周期番号 | 色 |
|---|---|---|
| **第一期** | 1〜8 | `#6f9ce8` |
| **第二期** | 9〜16 | `#e8776f` |
| **第三期** | 17〜19 | `#e8c96f` |

判定は `periodGroupOf(period)`（8以下＝第一期、16以下＝第二期、それ以外＝第三期）。

### 月（29〜30個）

月の中の日には `LUNAR_ACTIONS`（発生・始動・試行…虚無）という30個の作用名がある。
こちらは今のところグループ分けされていない。

## このアプリ側で対応済みのこと

**光り方を揃える**

- **「RONDOの光」ボタン**… 押すと上の配合に一発で揃う。
  点と環の色7つ（青・赤・金・生成り・青灰・金茶・淡青）に加えて、
  グループの色6つ（下降期・上昇期・編成月・周期第一〜第三期）も並べてある
- **「重なりを明るくする(加算合成)」**… `mix-blend-mode: plus-lighter`。
  three.js の AdditiveBlending と同じ見え方になる

**ピクトグラムを作る**

- **下絵**… 「白紙にする」で真っ白な台紙を出し、筆・直線・円・四角・消しゴムで下絵を描く。
  写真の上に描き足すこともできる。線はベクタで持っているので「一つ戻す」が効く
- 下絵と点描は**同じ枠に重ねて**表示される（以前は参照画像だけ枠いっぱいで数%ずれていた）

**RONDO へ渡す**

- **「RONDOへ渡す」**… 種類とバリアントを一覧から選び、
  「SVGコードをコピー」（`?edit=1` の貼り付け欄用）か
  「layout.json用のJSONをコピー」（`types` に足す形）で出す。
  キーの一覧は RONDO の `src/labelTypes.js` の写しを持っている
- **「連作」**… 選んだ種類のバリアントをスロットとして並べる。
  1つ作るたびに「取り込む」で溜め、最後に「まとめて書き出す／コピー」で
  13個ぶんの JSON を一度に出す。`?edit=1` に一括取り込みが無いのでJSONで渡す

対応の要点:

- `haloSpread`（光の広がり）が **暈と芯の半径比** にあたる。`1 ÷ 0.32 ≒ 3.1` で RONDO に揃う
- SVG は `<g fill=暈色 filter=dotBlur…>` と `<g fill=芯色 filter=coreBlur…>` の2層構造
- **SVG内の id とクラス名には固有の接尾辞が付く**（`dotBlur1a2b3c` のように）。
  `url(#…)` も `<style>` の中身も貼り付け先の文書全体で共有されるので、
  素の名前のままだと13枚が互いのぼかし具合やゆらめきを奪い合う。接尾辞は
  乱数の種から作っているので、同じ絵なら何度書き出しても同じ名前になる
- 点の配置は**種(seed)から再現できる**。連作スロットを開き直すと前と同じ点が戻る。
  総入れ替えしたいときだけ「再生成」を押す

## ピクトグラムを作るときの手順

RONDO の各区分名（下降高次①〜上昇高次②の12個＋編成月）を、
文字ではなく**ピクトグラム**で見せる。作り方は次のとおり。

1. **「RONDOへ渡す」で種類を選ぶ**（区分名なら `一年の区分名`）。
   下の「連作」に13個＋共通のスロットが並ぶ
2. **グループの色を押す**（下降期／上昇期／編成月）。光り方がRONDOと揃う
3. **「白紙にする」→ 下絵を描く**。細い線だと点が乗りにくいので、
   線は太めに、`点の数` は多めにするとよい
4. **どのスロットに入れるか選んで「いまの絵を取り込む」**
5. 2〜4 を繰り返し、最後に **「まとめて書き出す」**

グループ全部に同じ1枚でよければ、スロットの
**「（この種類ぜんぶ共通）」** に入れれば足りる。
`types[kind]` が土台で `types[kind/variant]` がそれを上書きする作りなので、
共通の絵を1つ置いて、違うものだけ個別に上書きできる。

RONDO 側の受け皿:

- `src/layout.json` … どの種類の要素にどの絵を割り当てるかの設定ファイル
- `?edit=1` … その設定を編集するアプリ（本体に同居している）
- 割り当ての単位は「札のID」ではなく **「種類（kind）＋バリアント（variant）」**。
  日付によって区分が12個か13個に変わるため、IDに紐づけると崩れるから

## RONDO の layout.json の書式

RONDO へ受け渡すスニペットを自動生成するなら、**この書式に合わせないと読めない**。

`src/layout.json` の `types` に、キーと設定を1つ足す形。

```json
"yearSegment/dk1": {
  "render": "svg",
  "svg": "<svg viewBox=\"0 0 280 420\" ...>...</svg>",
  "layer": "segments",
  "offset": [0, 0],
  "scale": 1,
  "rotation": 0,
  "anchor": "center",
  "fallbackToText": true
}
```

- `render` … `"text"`（既定）/ `"svg"` / `"image"` / `"hidden"`
- `svg` … インラインSVG文字列。`render:"svg"` のときに使う
- `image` … `public/` からのパス。`render:"image"` のときに使う
- `offset` … 投影位置からのズレ（px）
- `rotation` … 度
- `anchor` … `center` / `top` / `bottom` / `left` / `right`
- `fallbackToText` … 絵が未設定なら文字を出す（既定 true）

**キーの作り方**は `種類` または `種類/バリアント`。
`種類` だけ書くとその種類ぜんぶの共通設定になり、`種類/バリアント` がそれを上書きする。
つまり **グループ共通の設定を1行で書ける**。

### 種類とバリアントの一覧

| 種類 | バリアント |
|---|---|
| `consciousWorld` | なし |
| `realWorld` | なし |
| `zone` | `koji` / `seishin` / `kitei` |
| `yearSegment` | `dk1` `dk2` `ds1` `ds2` `dt1` `dt2` `jt1` `jt2` `js1` `js2` `jk1` `jk2` / `hensei` |
| `segmentDate` | 同上（区分の新月日付） |
| `segmentLunar` | 同上（区分の旧暦） |
| `solstice` | `summer` / `autumn` / `winter` / `spring` |
| `innerAction` | `再生` `放出` `調整` `始動` `露呈` `受容` `成長` `交流` |
| `outerAction` | 同上（8つの作用名） |
| `cycleResult` | 同上（8つの作用名） |
| `cycleNumber` | `"1"` 〜 `"19"` |
| `moonNew` | なし |
| `moonPhase` | `firstQuarter` / `full` / `lastQuarter` |
| `lunarDay` | `"1"` 〜 `"31"` |
| `lunarAction` | `"0"` 〜 `"29"` |

区分キーの読み方: `d`=下降 / `j`=上昇、`k`=高次 / `s`=精神 / `t`=基底、末尾の数字が①②。
つまり `dk1` = 下降高次①、`jt2` = 上昇基底②。

### 作用の8種類（いちばん割のいい描き先）

RONDO には「19周期作用表」が入っている。各周期には

- **内的作用**（意識世界のサークルの中央に出る。周期全体で同じ）
- **外的作用**（現実世界のサークルの左右に出る。下降期と上昇期で違う）
- **反映結果**（周期番号の下に出る。その周期全体に現れる結果）

の3つがあり、そこに入る言葉は**どれも次の8つしかない**。

    再生 ・ 放出 ・ 調整 ・ 始動 ・ 露呈 ・ 受容 ・ 成長 ・ 交流

つまり **8枚描けば19周期ぶん全部まかなえる**。19個や13個を描く必要はない。
`innerAction` / `outerAction` / `cycleResult` の3つの種類で同じ8枚を使い回せるので、
まず作るならここが効率がいい。

作用名のうしろには強度（`+1`〜`+4`）や強さ（極・強・並・中）が付くが、
これは文字のまま RONDO 側が添えるので、絵には入れなくてよい。

反映結果だけは**文字の色に意味がある**（内的＝青／外的＝赤／内外＝二色）。
絵に差し替えるとその区別が消えるので、色分けを残したいなら文字のままにしておく。

### レイヤーID

`world` / `zones` / `lunarDates` / `dates` / `solstice` / `segments` / `actions` / `cycles` / `lunar`

## 受け渡しの流れ

1個だけ渡す:

    このアプリで作る → 「SVGコードをコピー」
      → RONDO の ?edit=1 で種類を選び、表示のしかたを SVG にして貼る

まとめて渡す:

    連作スロットに溜める → 「まとめて書き出す」（rondo-layout-types.json）
      → rondo-cosmos の src/layout.json の types にその中身を足す

将来もっと滑らかにするなら、このアプリを RONDO に同居させて
（`?trace=1` のような形）、作ったその場で割り当てられるようにする案がある。
ただし現状は別リポジトリで動いていて公開もされているので、無理に混ぜていない。

## 注意

- このアプリは `index.html` 1枚にHTML・CSS・JSが全部入っている（ビルド不要）
- 動作確認は静的サーバーで開けばよい（ローカルファイル直開きだと動かない場合がある）
- 設定はブラウザに保存される（`現在の設定を保存` → `書き出す` / `読み込む`）
- 連作スロットもブラウザに保存される（`stipple-tracer:rondo-slots:v1`）。
  下絵つき1個でおよそ40KB、13個で500KB前後。プライベートウィンドウでは保存が
  効かないので、その場合は「まとめてコピー」で持ち出す
- 点の数を増やすとSVGがそのまま重くなる。書き出しサイズは画面下に常に出しているので、
  `layout.json` に13個埋め込むことを考えて見ながら決める
