# 点描トレーサー

画像や下絵から「光の点描画」のSVGを作る、静的サイト。
作った絵は姉妹アプリ **RONDO 時の座標**（`ym-rondo/rondo-cosmos`）に載せる。
**トレーサーは「絵を作る側」、RONDO は「絵を置く側」。**

## 最初に読むもの

- **`RONDO連携メモ.md`** … RONDO側の事情。光り方の配合、色の対応表、
  `layout.json` の書式、種類(kind)/バリアント(variant)の一覧、ピクトグラムの作り方。
  RONDO がらみを触るなら先に読む。
- `README.md` … このアプリ自体の説明とファイル構成

## 触るときの前提

- アプリ本体は `index.html` 1枚にHTML・CSS・JSが全部入っている。**ビルドは無い**
- 外部ライブラリは使わない（読み込むのは Google Fonts だけ）
- 動作確認は静的サーバーで開く（`file://` の直開きだと動かない場合がある）
- アイコンは `node makeicon.js` で作り直す。PNGとSVGが同時に出る
- 検索結果に出さない方針。`robots.txt` に `Disallow` を**書かない**理由は
  README の「検索結果に出さないようにしている」に書いてある。消さないこと

## いちばん壊しやすいところ

**光り方**。RONDO は three.js の光の点でできた空間で、そこへ別の光り方の絵を
持ち込むとその絵だけ浮いて「貼り付けた」ように見える（実際に一度失敗している）。
**白く小さい芯＋色のついた広い暈**という組み立てが肝。連携メモの
「いちばん大事な制約」を読んでから触る。

**書き出すSVGの id とクラス名**。`dotBlur…` のように乱数の種から作った接尾辞が
付いている。`url(#…)` も `<style>` の中身も貼り付け先の文書全体で共有されるため、
素の名前に戻すと、RONDO に並べた複数の絵が互いのぼかし具合やゆらめきを奪い合う。

## RONDO 側のソースについて

`src/layout.json` や `src/labelTypes.js` といったパスが連携メモに出てくるが、
これらは **rondo-cosmos 側**のもので、このフォルダの中には無い。
このPCにも置いてあるが、フォルダの場所は変わることがある。
見当たらなければ https://rondo-cosmos.vercel.app と GitHub を見る。
